# Environment Variables Template for Praxis Initiative
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# =============================================================================
# WordPress Configuration - MCP Integration
# =============================================================================

# WordPress Site URL
WORDPRESS_URL=https://praxisinitiative.org

# WordPress Admin Credentials (use application passwords for security)
# Create application password at: WordPress Admin > Users > Your Profile > Application Passwords
WORDPRESS_USERNAME=your-username-here
WORDPRESS_PASSWORD=your-wordpress-app-password-here

# =============================================================================
# Elementor MCP Configuration
# =============================================================================

# WordPress URL for Elementor integration
WP_URL=https://praxisinitiative.org

# WordPress Application User (same as above, but different variable name for Elementor MCP)
WP_APP_USER=your-username-here
WP_APP_PASSWORD=your-wordpress-app-password-here

# =============================================================================
# MCP Server Authentication
# =============================================================================

# MCP Relay Server Authentication Token
# Generate a secure random token (32+ characters recommended)
# Example generation: openssl rand -base64 32
MCP_AUTH_TOKEN=your-secure-random-token-here

# =============================================================================
# GitHub Integration (if needed)
# =============================================================================

# GitHub Personal Access Token for repository operations
# Create at: GitHub Settings > Developer settings > Personal access tokens
GITHUB_TOKEN=your-github-token-here

# =============================================================================
# Development Configuration
# =============================================================================

# Development environment identifier
NODE_ENV=development

# Local development ports
MCP_RELAY_PORT=3000
HTTP_SERVER_PORT=8080

# =============================================================================
# Security Notes
# =============================================================================
#
# 1. NEVER commit this file with actual values to version control
# 2. Use strong, unique passwords and tokens
# 3. Rotate credentials every 90 days
# 4. For production, use dedicated secrets management services
# 5. WordPress application passwords are safer than regular passwords
# 
# WordPress Application Password Setup:
# 1. Log into WordPress admin
# 2. Go to Users > Your Profile
# 3. Scroll to "Application Passwords"
# 4. Enter "Praxis MCP Integration" as name
# 5. Click "Add New Application Password"
# 6. Copy the generated password (format: abcd efgh ijkl mnop)
# 7. Use this password for WORDPRESS_PASSWORD and WP_APP_PASSWORD
#
# Secure Token Generation:
# - Linux/macOS: openssl rand -base64 32
# - Node.js: require('crypto').randomBytes(32).toString('base64')
# - Online: Use a reputable password generator (32+ characters)
#
# =============================================================================